server:
  port: 3377
spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        # Nacos作为服务注册中心地址
        server-addr: localhost:8848
      config:
        # Nacos作为服务配置中心地址
        server-addr: localhost:8848
        file-extension: yaml
        group: TEST_GROUP
        namespace: fee9915c-0cb2-463f-b5ab-d19e200a4ca2

